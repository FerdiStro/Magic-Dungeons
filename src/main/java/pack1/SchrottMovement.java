package pack1;

public class SchrottMovement {/*
	Timer movement;
	private int temp = 0;

	public SchrottMovement() {

		for (int i = 0; i <= 4; i++) {

			pack1.Var.schrottx1[i] = 20 + temp;
			pack1.Var.schrotty1[i] = -200 - temp;

			temp += 160;

		}

		pack1.Var.schrottx2[0] = 266;
		pack1.Var.schrottx2[1] = 532;
		pack1.Var.schrotty1[0] = 1000;
		pack1.Var.schrotty2[1] = 1500;

		pack1.Var.schrottx3 = 375;
		pack1.Var.schrotty3 = -4000;

		movement = new Timer();
		movement.scheduleAtFixedRate(new TimerTask() {

			@Override
			public void run() {

				for (int i = 0; i <= 4; i++) {
					pack1.Var.schrotty1[i] += pack1.Var.schrottspeed1[i];

					if (pack1.Var.schrotty1[i] >= pack1.Var.screenheight) {
						pack1.Var.schrotty1[i] = -350;
					}

				}

				for (int i = 0; i <= 1; i++) {
					pack1.Var.schrotty2[i] += pack1.Var.schrottspeed2[i];

					if (pack1.Var.schrotty2[i] >= pack1.Var.screenheight) {
						pack1.Var.schrotty2[i] = -1500;
					}

				}
				
				pack1.Var.schrotty3 +=pack1.Var.schrottspeed3;
				if(pack1.Var.schrotty3>=pack1.Var.screenheight){
					pack1.Var.schrotty3 =-4000;
				}
				
			}

		}, 0, 9);
	}
*/
}
